// BẮT BUỘC: Dòng này phải là dòng đầu tiên
package controller;

// (Sau này chúng ta sẽ import OrderDAO, ProductDAO...)

/**
 * Lớp này là cầu nối cho các chức năng liên quan đến Bán hàng.
 * (Hiện tại chỉ cần khai báo lớp để hết lỗi build)
 */
public class SalesController {
    
    public SalesController() {
        // (Sau này sẽ khởi tạo OrderDAO, ProductDAO tại đây)
    }

    // (Sau này sẽ thêm các hàm như createOrder, addProductToCart...)
}

